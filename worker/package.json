{
  "name": "readline-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The reader half of readline: fetches a link and serves it as plain HTML.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "node --test test/",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
